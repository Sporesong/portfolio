{
  "name": "y",
  "version": "1.0.0",
  "description": "cv-site-sporesong",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel ./*.html",
    "predeploy": "rm -rf dist && parcel build ./*.html --public-url https://github.com/Sporesong/portfolio",
    "deploy": "gh-pages -d dist"
  },

  "author": "suvi sivula",
  "license": "MIT",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.3"
  }
}

